import styled from "styled-components";

const StyledText = styled.h1`
  font-weight: 400;
  font-size: 29px;
  line-height: 37px;

  strong {
    font-weight: 700;
  }
`;

const Text = StyledText;

export default Text;
